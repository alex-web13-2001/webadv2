# WB Ads Realtime Dashboard

Минималистичное веб-приложение для визуализации аналитики рекламных кампаний Wildberries в реальном времени.

## 🎯 Возможности

- ✅ Проверка API ключа Wildberries
- ✅ Загрузка списка рекламных кампаний
- ✅ Детальная статистика по кампаниям (Views, Clicks, CTR, CPC, CPM, Orders)
- ✅ Аналитика поисковых кластеров
- ✅ Интерактивные графики в реальном времени
- ✅ Адаптивный дизайн

## 🛠 Технологии

### Backend
- Node.js + Express + TypeScript
- axios для запросов к WB API
- CORS middleware

### Frontend
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Custom chart components (без внешних библиотек)

## 📋 Требования

- Node.js 20+
- npm или yarn
- API ключ Wildberries

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/alex-web13-2001/webadv2.git
cd webadv2
```

### 2. Установка Backend

```bash
cd backend
npm install
cp .env.example .env
```

Настройте `.env` файл:
```
PORT=3001
NODE_ENV=development
WB_API_BASE_URL=https://advert-api.wildberries.ru
```

Запуск:
```bash
npm run dev
```

Backend будет доступен на http://localhost:3001

### 3. Установка Frontend

```bash
cd frontend
npm install
cp .env.local.example .env.local
```

Настройте `.env.local` файл:
```
NEXT_PUBLIC_API_URL=http://localhost:3001
```

Запуск:
```bash
npm run dev
```

Frontend будет доступен на http://localhost:3000

## 📖 Использование

1. Откройте http://localhost:3000
2. Введите ваш API ключ Wildberries
3. Нажмите "Проверить API ключ"
4. После успешной проверки загрузятся ваши кампании
5. Выберите кампанию из списка
6. Просматривайте статистику кампании в разделе "Обзор статистики"
7. Для просмотра аналитики поисковых кластеров введите артикул товара (nm_id) и нажмите "Загрузить"

## 🏗 Структура проекта

```
webadv2/
├── backend/           # Node.js API сервер
│   ├── src/
│   │   ├── routes/    # API роуты
│   │   ├── services/  # Бизнес-логика
│   │   ├── middleware/# Middleware функции
│   │   ├── types/     # TypeScript типы
│   │   └── server.ts  # Входная точка
│   └── package.json
├── frontend/          # Next.js приложение
│   ├── app/          # App Router
│   ├── components/   # React компоненты
│   ├── lib/          # Утилиты
│   └── package.json
└── README.md
```

## 🔌 API Endpoints

### POST /api/test-key
Проверка валидности API ключа

### GET /api/campaigns
Получение списка кампаний

### GET /api/campaign/:id/stats
Получение статистики кампании

### POST /api/campaign/:id/clusters
Получение аналитики поисковых кластеров

## ⚠️ Важно

- Приложение НЕ использует базу данных
- Все данные загружаются в реальном времени
- API ключ хранится только в памяти браузера
- История данных не сохраняется

## 📝 Лицензия

MIT

## 👨‍💻 Автор

alex-web13-2001
